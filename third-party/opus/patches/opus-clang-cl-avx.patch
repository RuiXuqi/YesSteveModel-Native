diff --git a/CMakeLists.txt b/CMakeLists.txt
index fcf034b..da30675 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -522,8 +522,10 @@ if(NOT OPUS_DISABLE_INTRINSICS)
         add_sources_group(opus lpcnet ${dnn_sources_avx2})
       endif()
       target_compile_definitions(opus PRIVATE OPUS_X86_MAY_HAVE_AVX2)
-      if(MSVC)
+      if(MSVC AND NOT MSVC_CLANG_CL)
         set(AVX2_FLAGS "${AVX2_FLAGS} /arch:AVX2")
+      elseif(MSVC AND MSVC_CLANG_CL)
+        set(AVX2_FLAGS "${AVX2_FLAGS} /clang:-mavx2 /clang:-mfma /clang:-mavx")
       else()
         set(AVX2_FLAGS "${AVX2_FLAGS} -mavx2 -mfma -mavx")
       endif()
